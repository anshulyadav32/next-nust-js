# NextAuth Configuration
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=your-secret-key-here-change-this-in-production

# Database Configuration
DATABASE_URL=\
postgresql://nextauth_user:nextauth_password@localhost:5432/nextauth_backend\

# Google OAuth (optional - leave empty if not using)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# GitHub OAuth (optional - leave empty if not using)
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# Email Configuration (for verification and password reset)
# For development, you can use Ethereal Email (run: npm run create-test-email)
# For production, use your actual SMTP settings
EMAIL_HOST=smtp.ethereal.email
EMAIL_PORT=587
EMAIL_USER=your-email-user
EMAIL_PASSWORD=your-email-password
EMAIL_FROM=noreply@yourapp.com

# Environment
NODE_ENV=development
