# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: fullstack-auth
metadata:
  template: fullstack-auth@0.0.1-beta
services:
  backend:
    project: ./backend
    language: ts
    host: containerapp
  frontend:
    project: ./frontend
    language: ts
    host: containerapp
